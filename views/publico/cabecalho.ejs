<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Galeria de Arte</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    /* Ajustes específicos para a área pública */
    .public-hero {
        background-color: var(--color-white);
        border-bottom: 4px solid var(--color-primary);
        padding: 4rem 2rem;
        margin-bottom: 2rem;
        text-align: center;
    }
    .art-card {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        height: 100%;
        border: none;
        background-color: var(--color-white);
    }
    .art-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(130, 94, 101, 0.2);
    }
    .art-img-container {
        height: 250px;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: #f8f9fa;
    }
    .art-img-container img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }
    .date-badge {
        background-color: var(--color-accent);
        color: white;
        padding: 5px 10px;
        border-radius: 4px;
        font-weight: bold;
    }
  </style>
</head>
<body>

  <nav class="navbar navbar-expand-lg navbar-dark navbar-custom mb-0">
    <div class="container">
      <a class="navbar-brand" href="/site"><i class="fa fa-palette"></i> Galeria Virtual</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/site">Início</a></li>
          <li class="nav-item"><a class="nav-link" href="/site/galeria">Galeria</a></li>
          <li class="nav-item"><a class="nav-link" href="/site/eventos">Agenda</a></li>
          <li class="nav-item"><a class="nav-link" href="/"><i class="fa fa-lock"></i></a></li>
        </ul>
      </div>
    </div>
  </nav>